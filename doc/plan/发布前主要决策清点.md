# 发布前主要决策清点

> 非设计文档，决策清点清单，无需审查。
> 来源：7 棵任务树（active `longops-task-integration`/`multitrack-resource-lineage` + archive `base-repo-update-save-hazard`/`frontend-extension-rename`/`plugin-convention-release-prep`/`rich-article-resource`/`workset-merge`）的「决策与约束」节与节点派生说明。
> 目的：为 epic「主程序主要决策定稿（发布前）」提供工作清单——已定稿的确认覆盖、隐晦的补文档化、悬而未决的逐项拍板。

## 一、悬而未决（发布前需逐项拍板——定稿 epic 的主要工作面）

| # | 决策项 | 现状 | 发布前是否需定稿 |
|---|---|---|---|
| W1 ✅ | **tag 体系演化（namespace 关联级属性）** | **定稿（2026-08-06）**：纳入发布版。方向 b 已定（I 节点），实施（原 I' 延后）转正为发布前任务。依赖 C（W2 修复）先完成。实施任务 → 树节点 D | doc/plan/tag体系演化方案.md |
| W2 ✅ | **tag_type/author_type 值冲突（确凿 bug）** | **定稿（2026-08-06）**：确凿 bug（非两套合法语义）。`constant.OriginType`(LOCAL=0/SITE=1, base 正统) vs `work/service.go` 入库字面量(1/2, 偏离)；tag_type=1/author_type=1 历史歧义。**统一到 constant(0/1)**：work 字面量 + AuthorType 常量回归 constant；历史数据用 LocalTagID/SiteTagID（author 同）Valid 消歧迁移。修复任务 → 树节点 C |
| W3 ✅ | **插件完整沙箱（D'）** | **定稿（2026-08-06）**：发布范围=开放不可信第三方生态，**接受 D' 缺口发布**（沙箱延后）。落实底线：发布说明 + 安装知情同意须充分告知"无沙箱、完整宿主能力、风险自负"；**待验证** D 节点知情同意弹窗文案充分性。D' 作为后续独立 epic，不阻塞首发 |
| W4 ✅ | **合并异步化（longops 阶段1）** | **定稿（2026-08-06）**：发布前做。激活 longops 树 C 节点（阶段1 异步化，原 park）：合并挪 goroutine + ffmpeg 进度回调 + cancel。解决同步阻塞 IPC。实施任务 → 树节点 E（跨树 longops） |
| W5 ✅ | **插件崩溃自动恢复（F）** | **定稿（2026-08-06）**：延后。发布版接受手动重激活（正确性无碍，UX 项）。F 作为后续 epic |
| W6 ✅ | **leaf/独立任务创建路径回归根治（N）** | **定稿（2026-08-06）**：发布前做。激活 plugin-convention N 节点（原延后）：applyCreateResponse 单点权威（stream/array 双路径统一）+ 回归测试 + 不变量守护。实施任务 → 树节点 F |
| W7 ✅ | **多格式文档查看器（T2）** | **定稿（2026-08-06）**：延后。现成文档作为 document 资源存储，查看靠下载/系统默认程序打开。T2 作为后续 epic |

## 二、隐晦待文档化（决策已落地但散落 memory/plan，未进 CLAUDE.md/rules 主体）

| # | 决策项 | 现状载体 | 待补 |
|---|---|---|---|
| H1 | **SQLite MaxOpenConns=1**（事务内误用 GORM() 死锁） | memory `maxopenconns-transaction-deadlock` | 进 database.md / 配置说明，作为发布版连接池约定 |
| H2 | **store_type 自定义角色边界**（G'） | G 开放了 ResourceTypeRegistry（插件自定义资源类型），但 store_type 自定义角色仍封闭 | 在 resource-type-spec.md 明确"store_type 7 角色封闭、插件自定义角色延后"的发布版边界 |
| H3 | **任务执行需手动触发**（无自动执行/恢复） | memory `task-resume-manual-trigger` | 进 task 模块 README / 用户文档（发布版用户需知） |
| H4 | **任务状态以资源为准** | memory `task-state-by-resource` | 进 task 模块 README |
| H5 | **独立/叶子任务控制（classifyTaskUnit）** | memory `standalone-task-pause-regression` + `leaf-task-regression-hotspot` | 进 task 模块 README（高回归区警示） |

## 三、已文档化（确认定稿，发布版依据已就位）

**资源模型与类型规约**
- ResourceType 规约体系（封闭枚举 + 严格识别 + 基数 Roles + PrimaryRoles 后端派生）— `doc/resource-type-spec.md`
- store_type 重构（去 main，7 角色 + merged）— `doc/resource-type-spec.md`
- 多板块 generation 区分（downloaded 可续传 / derived 一次性）— backend.md / plugin-dev-guide
- ResourceTypeRegistry 开放（插件自定义类型）— plugin-dev-guide「自定义资源类型声明」
- 图文紧密结合资源（article = document + N image）— `doc/plan/rich-article-resource-plan.md`

**store 命名与路径**
- 落盘命名规约（`<bas>_<role>_<seq>[_<desc>].<ext>`）— `doc/store-naming-convention.md`
- StoreSeq = 同 role 内序号 — store-naming-convention / database.md
- 路径存储规范（workDir 相对、store/resource 等）— database.md

**任务执行与调度**
- 「一任务一 goroutine」+ reader 不跨 RPC — plugin-dev-guide / memory
- 磁盘 stat 唯一权威 — plugin-dev-guide
- 优雅暂停（排空在途 chunk）— plugin-dev-guide
- 任务板块指定协议（InvolvedRoles=universe hint / StoreRoles=selection / Start=truth）— plugin-dev-guide
- 任务执行只处理插件本次声明板块、merged 透明保留 — 多轨方案

**合并（merge）**
- 合并产物恒挂 merged（优先级 merged>main）— 多轨方案
- 物理纳入=复制关联（静态快照、不可撤回）/ 逻辑关联=多父 DAG+环路检测 — workset-merge plan
- 作品集父子关系与子集排序（QueryWorkSetRelations 拉取范式）— plugin-dev-guide

**数据访问层**
- BaseRepository 写方法语义（Create/Save/Updates）— database.md / backend.md
- 零值合法字段用 sql.Null* — database.md
- 接口命名分层（Repository GORM 名 / Service 业务名）— backend.md
- 批量更新单条 SQL / 消除 N+1 — backend.md

**插件契约**
- contractVersion 版本协商（安装/加载双校验）— plugin-dev-guide
- 能力声明化（capabilities 封闭枚举）— plugin-dev-guide
- 插件前端契约 render.Context 独立断链 — plugin-dev-guide
- 共享枚举单源（SDK contract 子包）— plugin-dev-guide
- 插件信任模型最小集 — plugin-dev-guide / rules
- 配置 schema 迁移（configSchemaVersion）— plugin-dev-guide
- plugin_data 格式版本由插件自管（三版本轴独立）— plugin-dev-guide / `doc/plan/任务插件版本绑定方案.md`
- 插件扩展点 FrontendExtension 7 种平级（前端二分）— rules / plugin-dev-guide

**前端架构**
- 主题令牌体系（--app-* 令牌）— rules / tokens.css
- 状态色板（语义 tone、主题驱动）— rules
- 插件前端故障隔离（PluginBoundary component-prop / AppIcon 数据边界）— rules / plugin-dev-guide
- 向导框架（useTourCenterStore + TourOverlay）— rules

**基础设施**
- 日志两阶段初始化 + 轮转 — CLAUDE.md
- 数据库 GORM AutoMigrate — CLAUDE.md
- 配置两层合并（嵌入 default + 磁盘覆盖）— CLAUDE.md
